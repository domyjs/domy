import { DomySpecialHelper } from '../types/Domy';

/**
 * Give the data
 * @param domy
 * @returns
 *
 * @author yoannchb-pro
 */
export function $data(domy: DomySpecialHelper) {
  return domy.state.data;
}
